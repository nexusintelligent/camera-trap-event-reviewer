# 快速模式效能驗證（2026-08-21）

測試資料固定為同一批 30 個事件，每個事件只使用 Photo1、Photo3，共 60 張照片。偵測分類門檻維持 `0.15`，結果輸出門檻維持 `0.01`。

| 指標 | 修改前 | 常駐 Worker（冷快取） | 常駐 Worker（暖快取） |
| --- | ---: | ---: | ---: |
| Python subprocess | 30 | 1 | 0（沿用常駐程序） |
| MegaDetector 載入 | 60 次 | 1 次 | 0 次（沿用已載入模型） |
| 送入照片 | 60 | 60 | 60 |
| 實際推論照片 | 60 | 60 | 0 |
| 30 組總時間 | 711.78 秒 | 338.73 秒 | 0.024 秒 |
| 每張平均總時間 | 11.863 秒 | 5.646 秒 | 0.0004 秒 |
| MegaDetector 推論 | 每事件平均 21.621 秒（舊紀錄未拆解碼） | 317.220 秒 | 0 秒 |
| 模型啟動 | 每次平均 9.688 秒，共 60 筆載入紀錄 | 10.288 秒（含匯入及單次載入） | 0 秒 |
| 照片解碼 | 舊紀錄未獨立計時 | 6.472 秒 | 0 秒 |
| 縮圖快取 | 無 | 4.476 秒，建立 60 張 | 0 秒 |
| 偵測快取 | 無 | 0 命中 | 60 命中 |
| SpeciesNet | 舊工作設定曾啟用 | 未載入 | 未載入 |
| 影片／影格 | 快速工作未送入 | 0／0 | 0／0 |
| 裝置 | CPU | CPU，batch size 1 | 快取命中 |

硬體診斷：`torch 2.13.0+cpu`，CUDA 不可用、CUDA 裝置數 0；Windows 顯示卡為 Intel(R) UHD Graphics。CPU 模式以 batch size 1 執行，若未來偵測到 CUDA GPU，Worker 會依顯示記憶體自動選擇 4、8 或 16。

冷快取總時間減少 52.41%，速度為舊架構的 2.10 倍。所有 SHA-256 都直接使用媒體清單的既有值，本次重算 0 次；原始照片未修改。
